1. Line 9 will print `values added: 20`
2. Line 13 will print `final result: 20`
3. Since `var` variables ignore block structure, it is likely that they will cause unintended behavior
4. Line 9 will print `values added: 20`
5. Line 13 will throw an error since there is no variable `result` in scope
6. The code will throw an error at line 7 since we're trying to reassign a `const`
7. The code will throw an error at line 7 since we're trying to reassign a `const`